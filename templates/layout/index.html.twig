{% extends 'base.html.twig' %}

{% block body %}
    {% if postIndex is defined %}
        <a href="{{ path('post_create') }}">Créer un article</a>
    {% endif %}
    {% for post in posts %}
        <div>
            <h2>{{ post.title }}</h2>
            <p>{{ post.content }}</p>
            <a href="{{ path('post_show', {'id': post.id}) }}">Voir l'article</a>
        </div>
    {% endfor %}
{% endblock %}